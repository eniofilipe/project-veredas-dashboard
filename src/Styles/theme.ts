import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#a5d5a5',
    },
    secondary: {
      main: '#a5d5a5',
    },
    text: {
      primary: '#000000',
      secondary: '#000000',
    },
  },
});

export default theme;
